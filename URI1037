#include <iostream>

using namespace std;

int main()
{
    float Value;
    cin >> Value;

    if(Value < 0.0000)
        cout << "Fora de intervalo\n";
    else if(Value >= 0.0000 && Value <= 25.0000)
        cout << "Intervalo [0,25]\n";
    else if(Value > 25.0000 && Value <=50.0000)
        cout << "Intervalo (25,50]\n";
    else if(Value > 50.0000 && Value <=75.0000)
        cout << "Intervalo (50,75]\n";
    else if(Value > 75.0000 && Value <=100.0000)
        cout << "Intervalo (75,100]\n";
    else if(Value > 100.0000)
        cout << "Fora de intervalo\n";

    return 0;
}
