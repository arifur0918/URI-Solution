#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    while(n--)
    {
      int base, power;
      cin>>base>>power;
      double Log=log10(base);
      Log=Log*power;
      cout<<floor(Log)+1<<endl;
    }
    return 0;
}
