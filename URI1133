#include <iostream>
using namespace std;

int main()
{
    int x, y, i;
    cin>>x;
    cin>>y;

    if(x<y)
    {
        for(i=x+1; i<y; i++)
            if((i-2)%5==0 || (i-3)%5==0)
                cout<<i<<endl;
    }

    else
    {
        for(i=y+1; i<x; i++)
            if((i-2)%5==0 || (i-3)%5==0)
                cout<<i<<endl;
    }

    return 0;
}
