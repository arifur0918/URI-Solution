#include<iostream>
using namespace std;
int main()
{
    int first, second, sum=0, i;

    cin>>first;
    cin>>second;

    if(first<second)
    {
        for(i=first; i<=second; i++)
            if(i%13!=0)
                sum=sum+i;
    }

    else
    {
        for(i=second; i<=first; i++)
            if(i%13!=0)
                sum=sum+i;
    }

    cout<<sum<<endl;
    return 0;
}
